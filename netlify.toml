[build]
publish = "public"
command = "npm run build-tw && hugo --gc --minify"

[context.production.environment]
HUGO_VERSION = "0.117.0"
HUGO_ENV = "production"
HUGO_ENABLEGITINFO = "true"
NODE_VERSION = "18"

[context.split1]
command = "npm run build-tw && hugo --gc --minify --enableGitInfo"

[context.split1.environment]
HUGO_VERSION = "0.117.0"
HUGO_ENV = "production"
NODE_VERSION = "18"

[context.deploy-preview]
command = "npm run build-tw && hugo --gc --minify --buildFuture -b $DEPLOY_PRIME_URL"

[context.deploy-preview.environment]
HUGO_VERSION = "0.117.0"
NODE_VERSION = "18"

[context.branch-deploy]
command = "npm run build-tw && hugo --gc --minify -b $DEPLOY_PRIME_URL"

[context.branch-deploy.environment]
HUGO_VERSION = "0.117.0"
NODE_VERSION = "18"

[context.next.environment]
HUGO_ENABLEGITINFO = "true"

[[redirects]]
from = "/npmjs/*"
to = "/npmjs/"
status = 200
